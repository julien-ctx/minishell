gerer le slash et le .
leaks pour plus d'un argument
continuer de req le prompt quand il y a une erreur de parsing comme &
seg fault quand on entre juste un backlash n
leak dans add_elmt
double free quand je mets sytem a la toute fin
